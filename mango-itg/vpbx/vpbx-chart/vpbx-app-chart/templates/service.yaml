apiVersion: v1
kind: Service
metadata:
  name: {{ include "vpbx-app-chart.fullname" . }}
  labels:
    {{- include "vpbx-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: web
  selector:
    {{- include "vpbx-app-chart.selectorLabels" . | nindent 4 }}