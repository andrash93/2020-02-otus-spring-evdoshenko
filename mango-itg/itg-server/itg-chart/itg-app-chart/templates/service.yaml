apiVersion: v1
kind: Service
metadata:
  name: {{ include "itg-app-chart.fullname" . }}
  labels:
    {{- include "itg-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: web
  selector:
    {{- include "itg-app-chart.selectorLabels" . | nindent 4 }}