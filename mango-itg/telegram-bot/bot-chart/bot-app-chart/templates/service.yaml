apiVersion: v1
kind: Service
metadata:
  name: {{ include "telegram-app-chart.fullname" . }}
  labels:
    {{- include "telegram-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: web
  selector:
    {{- include "telegram-app-chart.selectorLabels" . | nindent 4 }}